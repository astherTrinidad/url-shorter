import styled from "styled-components";

const ComponentStyled = styled.div`
  .page-container {
    min-height: 100vh;
    display: flex;
    flex-flow: column;
  }

  .App-logo {
    height: 10vmin;
  }
`;

export default ComponentStyled;
